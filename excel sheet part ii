#include <bits/stdc++.h>
using namespace std;

int main(){
	int t; cin>>t;
	while(t--){
	    string k; cin>>k;
	    long int column=0;
	    for(int i=0; i<k.length(); i++){
	        column = column + (k[k.length()-i-1]-'A'+1)*pow(26.0, float(i));
	    }
	    cout<<column<<endl;
	}
	return 0;
}
