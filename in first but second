#include <bits/stdc++.h>
#define f for(int i=0; i<n; i++)
#define ll long long int
using namespace std;

int main() {
	int t; cin>>t; while(t--){
	    int n,m; cin>>n>>m;
	    set<ll> count;
	    ll a[n] , c ; f cin>>a[i];
	    for(int i=0; i<m; i++){
	        cin>>c;
	        count.insert(c);
	    }
	    f{
	        if(count.find(a[i]) == count.end()){
	            cout<<a[i]<<" ";
	        }
	    }
	    cout<<'\n';
	}
	return 0;
}
